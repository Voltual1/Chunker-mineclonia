<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".BBQApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.BBQ.Launcher"
        android:supportsRtl="true"
        android:largeHeap="true"  
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">
        
        <meta-data
            android:name="license"
            android:value="gpl_3_0" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.CrashLogActivity"
            android:exported="false" />

        <!-- 注册多进程 WorkManager 服务，使其运行在独立的 :conversion 进程中 -->
        <service
            android:name="androidx.work.multiprocess.RemoteWorkerService"
            android:exported="false"
            android:process=":conversion"
            tools:node="merge" />

        <!-- 默认的 WorkManager 初始化器配置 -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="merge" />
        </provider>

    </application>
</manifest>